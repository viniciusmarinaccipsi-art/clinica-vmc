# PROMPT · Pacote 16.5c — Checagem breve, página Automonitoramento e menu "Suas 5 etapas" (index-dev)

Sistema Clínico Digital VMC · 26/09/2026 · Tema G · Pacote 16.5, fase 6. Primeira fase que redesenha telas. As 5 etapas continuam com o visual de hoje (isso é o 16.5d-2).

## Contexto
1. Ler `CLAUDE.md`, `docs/prompts/16.5/00_PANORAMA_16_5.md`, `docs/arquitetura.md` (seções "Automonitoramento" e "Sistema visual"), `docs/licoes-aprendidas.md` (79–84). `git pull`. Pré-requisitos fechados: 16.5a · 16.4.5 · 16.5b · 16.4.4 · 16.5d-1 · pendências (`83951a0`).
2. Só `index-dev.html` (+ `docs/design/icones.md` e `docs/arquitetura.md`). `index.html` e `Código.js` **intocados**; sem `clasp`. Pacote só de frontend: `git push` publica.
3. **Entrada do Design (rodada 3), já conferida pelo script** — pasta `../VMC-offline/16.5_entrada_design/rodada3/entrega/`: `02_telas.md` (AUT-02, AUT-03, AUT-03b, AUT-03c, AUT-12b), `03_componentes.md` (MenuEtapas, CartãoTipo), `01_png/` (AUT-02_automonitoramento.png, AUT-02_automonitoramento-primeiro-acesso.png, AUT-03_checagem-breve.png, AUT-03b_checagem-enviada.png, AUT-03c_suas-5-etapas.png, AUT-12b_rascunho-encontrado.png), `04_tokens.css` (4 tokens novos), `icones.js` (`i-lock` novo), `06_contrato_de_leitura_v3.md`, `00_LEIA-ME.md`, `CONFERENCIA_rodada3.md`. Brief: `../rodada3/BRIEF_rodada3_design.md`. A versão 25/09 do usuário (`../rodada3/referencia_25-09/V-01…V-03`) prevalece sobre a prancha em caso de conflito.
4. **Primeiro passo obrigatório:** copiar a pasta `entrega/` para `docs/design/16.5/rodada3/` (sem `support.js`, sem `01_pranchas_16.5_v3.dc.html`) e rodar `python scripts/conferir_entrega_16_5.py docs/design/16.5/rodada3` — item 0 IGUAL e item 1 = 45 · 225 · 45 · 3. Guardar a saída (gabarito ANTES).
5. Regras: DOM-first (o `catalogo.js` do Design **não entra no app**; textos de interface vêm da lista de textos + aceitos); tokens do `04_tokens.css` **mesclados** ao `:root` (nunca copiar por cima); zero cor fixa fora do `:root`; zero emoji; toque ≥ 44 px; piso 13/14 px; Figtree única; nenhuma classe CSS sem uso ao final; nenhum `console.log` com dado clínico; edições por `str_replace` com grep + `wc -l`; função substituída inteira → grep por fragmentos órfãos.

## Decisões que este prompt implementa (fechadas pelo usuário em 25–26/09)
- **D3** — o tipo de registro se escolhe na página Automonitoramento, **depois** da checagem.
- **D6** — nomes do humor: **Muito mal · Mal · Mais ou menos · Bem · Muito bem** (1→5). Os valores gravados continuam 1–5.
- **D10/decisão 5** — o menu das etapas continua, como tela própria (AUT-03c), **sem estado por etapa** e sem trilho.
- **D11** — primeiro acesso com cadeado; a checagem breve está sempre liberada.
- **Dúvidas do Design 1–6 (respondidas "ok" em 26/09):** (1) "Salvar e sair" no menu grava o rascunho com humor + tipo e volta à página Automonitoramento, que mostra o aviso de rascunho; (2) enquanto houver rascunho, o aviso **substitui** o bloco "QUE TIPO DE REGISTRO?"; (3) e (4) e (6) são do 16.5d-2 (registradas lá); (5) o Voltar (‹) do menu vai para a página Automonitoramento.
- **Checagem sozinha grava 1 linha.** Conferido no código real: `salvarAutomonitoramento` (Código.js) não exige `neg_preenchido`/`pos_preenchido`; só a tela de revisão avisa. Logo "Concluir só com a checagem de humor" envia a **mesma ação** com `humor_nivel`, `data_registro`, `hora_registro`, `humor_observacoes` (e `versao_formulario` como hoje), com os dois `*_preenchido` vazios. **Nenhuma coluna nova, nenhuma mudança em Código.js.** O que muda é o consumo dessa linha (Passo 6).

## Passos
1. **Tokens e ícone.** Mesclar ao `:root` claro/escuro de `index-dev.html` (e a `docs/design/tokens.css`) os 4 tokens novos: `--c-line-alpha`, `--c-scrim`, `--hit-thumb`, `--w-slider-desktop` (valores do `04_tokens.css` v3; os dois últimos ficam declarados para o 16.5d-2). Inserir `<symbol id="i-lock">` de `icones.js` no sprite; atualizar `docs/design/icones.md`.
2. **AUT-03 Checagem breve de humor** (`#sec-auto-passo1`, `data-barra-titulo="Checagem de humor"` → "Checagem breve de humor"): deixa de ser "Etapa 1 de 8" — remover o contador de etapas desta tela e de qualquer texto "de 8"; 5 faixas com número, rostinho `i-humor-N` e o nome **D6**, fundo `--c-humor-N-bg` e tinta `--c-humor-N-ink`; caixa descritiva do nível; data e hora com "alterar"; "Observações (opcional)" com a pergunta de contexto fora do campo (textos da prancha AUT-03 / lista de textos). Fecha em rótulo **"QUER CONTINUAR?"** → botão primário **"+ Iniciar novo registro"** (o "+" é o ícone `i-plus` do sprite, não texto) → botão secundário **"Concluir só com a checagem de humor"**. Humor obrigatório na própria tela (aviso junto das faixas, D9).
   - **Uma fonte para os nomes do humor:** hoje há 5 cópias (`mood-lbl` no HTML, `humorLbl` ~l. 10106, mapas ~l. 13158 e ~l. 14101, `HIST_HUMOR_LBL` ~l. 14404). Criar **uma** constante (ex.: `HUMOR_NOMES`) e fazer todos os consumidores lerem dela; grep final: zero ocorrências de "Muito negativo", "Negativo'", "Neutro'", "Positivo'", "Muito positivo" como rótulo de humor em `index-dev.html` (o par Registro Negativo/Positivo não é humor e fica).
3. **"Concluir só com a checagem"** → `autoEnviarRegistro()` com os dados da checagem e sem tipo → em `resp.ok`, tela **AUT-03b "Checagem enviada"** (nova seção, conforme prancha: confirmação, humor registrado com rostinho e hora, botões "Fazer um registro completo" → página Automonitoramento e "Voltar ao Início"). Falha → mesma faixa "Tentar de novo" do `chamarServidor`. Um novo registro depois disso começa **outra** checagem (a enviada não é reaproveitada).
4. **"+ Iniciar novo registro"** → página Automonitoramento (`#sec-automonitoramento`) com o humor da checagem guardado em `AUTO_STATE.dados` e visível no topo como linha "HUMOR · rostinho · nome · hora" com "editar" (volta à checagem com os valores preenchidos).
5. **AUT-02 Página Automonitoramento** (`#sec-automonitoramento`, barra "Automonitoramento" — sai "Módulo 2 — "): no topo o rótulo **"QUE TIPO DE REGISTRO?"** e os dois **CartãoTipo** (Registro Negativo, ameixa; Registro Positivo, verde-água; descrições do catálogo — copiar do HTML atual da tela "Tipo de registro", nunca do `catalogo.js`); abaixo, Painel de Evolução, Meus Registros e "Como Usar Este Registro" como hoje. O cartão "Iniciar Novo Registro" **sai**. Tocar num CartãoTipo **sem checagem nesta sessão** (sem `humor_nivel` em `AUTO_STATE.dados`) abre a checagem (AUT-03) primeiro e, no "+ Iniciar novo registro", volta a esta página com o tipo ainda por escolher; com checagem feita, vai direto ao menu (Passo 7). Se `02_telas.md` (AUT-02) descrever esse caso de outra forma, segue-se o `02_telas.md` e registra-se a diferença no relatório.
   - **Primeiro acesso (D11):** critério = nenhum registro gravado do paciente (o mesmo `algumNeg`/`algumPos` de `lerHistorico`, ~l. 12896) **e** "Como Usar" não aberto nesta sessão (flag em `sessionStorage`, padrão da escala 16.3 `ESC_FLAG_COMO_USAR`). Nesse estado: aviso âmbar com `i-lock` ("Antes do registro completo, leia o "Como Usar Este Registro". A checagem breve de humor já está liberada."), CartãoTipo apagados e não tocáveis (`aria-disabled`, sem `onclick`), "Como Usar" com borda em destaque. Abrir "Como Usar" grava a flag e libera ao voltar.
   - **Rascunho encontrado (AUT-12b, dúvida 2):** quando `autoSalvarRascunho()` tiver um rascunho na aba, o aviso "Você tem um registro em andamento…" com **"Continuar registro" / "Descartar"** ocupa o lugar do bloco "QUE TIPO DE REGISTRO?" (os cartões não aparecem até decidir). "Continuar" restaura `AUTO_STATE` e abre o menu (Passo 7) do tipo salvo; "Descartar" limpa o rascunho e mostra os cartões.
6. **Consumidores da linha "só checagem"** (`neg_preenchido` e `pos_preenchido` vazios) — grep e ajuste em cada um: Meus Registros (`~l. 12896` e o cartão: chip **"CHECAGEM DE HUMOR"** em `--c-humor-3-bg`/`-ink` no lugar de Negativo/Positivo, sem botão de detalhe das etapas), resumo/revisão (`~l. 14202–14290`: o aviso "Você ainda não preencheu nenhum registro" não se aplica ao caminho da checagem sozinha), Painel de Evolução (a linha entra no gráfico de humor como qualquer outra), infográfico/modal de humor (`~l. 10106`), visão do profissional (contagem: **separar checagens de registros** só se houver contador visível; senão, registrar como observação para o 16.7 — dashboard do profissional). Registro antigo com os dois vazios (se existir) passa a ser exibido como checagem.
7. **AUT-03c Menu "Suas 5 etapas"** — substitui `#sec-auto-menu-neg` e `#sec-auto-menu-pos` por **uma** seção (`#sec-auto-menu`, classe por tipo) — ou mantém as duas se a navegação (`data-barra-voltar`, `abrirSecao`) tornar a unificação mais cara; decidir e registrar. Conteúdo, conforme prancha AUT-03c e `03_componentes.md` MenuEtapas: barra "REGISTRO NEGATIVO/POSITIVO · Suas 5 etapas" com **"Salvar e sair"** (dúvida 1: grava rascunho com humor + tipo → página Automonitoramento com aviso de rascunho); bloco HUMOR (borda esquerda, `i-thermometer`, rostinho, nome D6, "· dd/mm às hh:mm", "editar"); "Toque em uma etapa para abrir."; um cartão por etapa (ícone da etapa em círculo 44 px, nome completo em caixa alta, a pergunta: "O que aconteceu?", "O que você sentiu?", "O que você sentiu no corpo?", "Quais pensamentos passaram pela sua mente?", "Como você reagiu a essa situação?"); rodapé fixo **"Começar: Situação ›"**. **Sem** estado por etapa, **sem** trilho. Voltar (‹) → página Automonitoramento (dúvida 5). Tocar num cartão abre a etapa correspondente (as etapas seguem como hoje).
8. **Sai:** a tela "Tipo de registro" (`#sec-auto-passo2`, `autoIrParaPasso2` e tudo que só ela usava — CSS incluído), os textos "Etapa N de 8" e "Módulo 2" no fluxo do automonitoramento. Grep final: `sec-auto-passo2` = 0, "de 8" no fluxo = 0, "Módulo 2 — Automonitoramento" = 0.
9. **Textos:** só os da lista (`docs/design/16.5/16.5_textos_interface.md`, coluna "Proposta") + aceitos: "Iniciar novo registro", "Concluir só com a checagem de humor", "QUER CONTINUAR?", "QUE TIPO DE REGISTRO?", "Suas 5 etapas", "Toque em uma etapa para abrir.", "Começar: Situação ›", "Continuar registro", "Descartar", "CHECAGEM DE HUMOR" (chip; texto novo deste prompt, motivo: elemento novo). Qualquer outro texto novo → parar e listar no relatório com "hoje → proposta → motivo".
10. **Marcador:** linha 2 → "Pacote 16.5c (data)".
11. **Validação:** `node --check`; tags balanceadas fora dos `<script>`; toda `var()` declarada; zero cor fixa fora do `:root`; zero emoji; zero classe sem uso (as classes da tela "Tipo de registro" removidas); toque ≥ 44 px nos CartãoTipo, faixas do humor, botões e cartões do menu; **gabarito DEPOIS** = ANTES (byte a byte); Playwright com Chrome visível 390×844 e 1280×800: login com sigla inválida até a mensagem do servidor, zero `pageerror`, comparando com `index.html`; sessão simulada do paciente de teste (`salvarSessao({...})`, sem senha) percorrendo: Início → Automonitoramento (primeiro acesso simulado com `sessionStorage` limpo e histórico vazio mockado → cadeado) → Como Usar → volta (cartões liberados) → CartãoTipo sem checagem → checagem (5 faixas D6) → "Concluir só com a checagem" → AUT-03b → Automonitoramento → checagem → "+ Iniciar novo registro" → CartãoTipo Negativo → menu AUT-03c → "Salvar e sair" → aviso de rascunho → "Continuar registro" → menu → "Começar: Situação" → etapa 1 (visual de hoje). Capturas de cada tela em `../VMC-offline/capturas_16_5c/` (390 e 1280). **Envio real de uma checagem sozinha com o paciente de teste VMC no publicado** e conferência da linha na aba Automonitoramento (só colunas: timestamp, versao, data, hora, humor_nivel, observações; `neg_preenchido` e `pos_preenchido` vazios; nenhum dado clínico no relatório).
12. **Publicar:** commit `Pacote 16.5c - Checagem breve, pagina Automonitoramento e menu Suas 5 etapas (index-dev)` · `git push` · esperar o Pages · repetir login inválido e o fluxo acima em `index-dev.html?v=165c`.
13. **Docs:** `docs/arquitetura.md` (fluxo novo: checagem → página → menu → etapas; linha "só checagem"; `HUMOR_NOMES` como fonte única; D11), `docs/design/icones.md` (`i-lock`). Se aparecer bug fora do escopo: registrar, não corrigir (vira 16.5c.1).
14. **Relatório** `../VMC-offline/PACOTE_16_5c_RELATORIO.md`: o que mudou (função:linha antes → depois), o que saiu (linhas removidas), textos novos com motivo, greps finais (zeros), gabarito igual, capturas, linha da checagem sozinha na planilha (colunas, sem conteúdo), decisão sobre uma ou duas seções de menu, diferenças em relação ao `02_telas.md`.
15. Mensagem final: "publicado" + 5 linhas de resultado.

## Checklist do usuário (aba anônima, `index-dev.html?v=165c`, paciente VMC)
- [ ] Automonitoramento abre com "QUE TIPO DE REGISTRO?" e os dois cartões; "Iniciar Novo Registro" não existe mais.
- [ ] Primeiro acesso (paciente sem registros): cadeado, cartões apagados, "Como Usar" em destaque; depois de abrir "Como Usar", cartões liberados. A checagem sempre funciona.
- [ ] Checagem: 5 faixas com rostinho e os nomes Muito mal · Mal · Mais ou menos · Bem · Muito bem; nada de "Etapa 1 de 8"; termina em "QUER CONTINUAR?" com os dois botões.
- [ ] "Concluir só com a checagem de humor" grava **1 linha** na planilha (aba Automonitoramento, `neg_preenchido` e `pos_preenchido` vazios) e aparece em Meus Registros como "CHECAGEM DE HUMOR" e no Painel de Evolução.
- [ ] "+ Iniciar novo registro" → página com o humor no topo → cartão do tipo → menu "Suas 5 etapas" com HUMOR, os 5 cartões e "Começar: Situação ›"; sem trilho e sem estado por etapa.
- [ ] "Salvar e sair" no menu → volta à página com "Você tem um registro em andamento…" no lugar dos cartões; "Continuar registro" reabre o menu; "Descartar" mostra os cartões.
- [ ] Voltar (‹) no menu vai para a página Automonitoramento.
- [ ] As 5 etapas continuam exatamente como antes (o visual delas é o 16.5d-2).
