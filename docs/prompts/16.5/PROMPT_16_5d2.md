# PROMPT · Pacote 16.5d-2 — Visual das 5 etapas: lista de subgrupos, barra deslizante, cabeçalho cumulativo, rodapé (index-dev)

Sistema Clínico Digital VMC · 26/09/2026 · Tema G · Pacote 16.5, fase 7. Redesenha o interior das 5 etapas dos dois tipos. O fluxo em volta (checagem → página → menu → etapas → revisão) já é o do 16.5c; Revisar/Enviado/Sair são o 16.5e; computador ≥ 900 px é o 16.5f (aqui só não pode quebrar).

## Contexto
1. Ler `CLAUDE.md`, `docs/prompts/16.5/00_PANORAMA_16_5.md`, `docs/arquitetura.md` (automonitoramento: seções `sec-auto-{sit,emo,fis,pens,comp}` com blocos `-a/-b`, chaves `data-grupo`/`data-item`/`data-tem-likert`, gravação "Item:nota" com `AUTO_SEPARADOR`), `docs/licoes-aprendidas.md` (79–84, e a lição do 16.4.1 sobre campo que muda de tipo). `git pull`. Pré-requisito: 16.5c fechado (`f5570f6`, testado pelo usuário em 26/09).
2. Só `index-dev.html` (+ `docs/arquitetura.md`, `docs/design/icones.md` se entrar ícone). `index.html` e `Código.js` **intocados**; sem `clasp`; `git push` publica.
3. **Entrada do Design (rodada 3, conferida):** `docs/design/16.5/rodada3/` — `03_componentes.md` §3 CabeçalhoCumulativo, §4 GrupoRecolhível, **§5 ListaSubgrupo**, **§6 BarraDeslizante**, §7 CampoTexto, §8 CartãoAjuda, §9 RodapéEtapa; `02_telas.md` AUT-04 a AUT-08, AUT-04p, AUT-08p; `01_png/` correspondentes; `06_contrato_de_leitura_v3.md` itens 15, 16, 17, 23. Referência do usuário (prevalece em conflito): `../VMC-offline/16.5_entrada_design/rodada3/referencia_25-09/V-04…V-08`.
4. **Primeiro passo obrigatório — gabarito ANTES:** `python scripts/conferir_entrega_16_5.py docs/design/16.5/rodada3` → item 1 = 45 · 225 · 45 · 3. Este pacote mexe exatamente onde o conteúdo clínico mora: **nenhum texto de item, grupo, descrição ou legenda muda**; ao final o gabarito tem de ser byte a byte o mesmo.
5. Regras: DOM-first (o `catalogo.js` do Design **não entra**; os textos já estão no HTML); contratos vivos intocados (`data-grupo`, `data-item`, `data-tem-likert`, "Item:nota", campo "Outro" por grupo, `versao_formulario`); tokens só do `:root` (os 4 novos já estão lá: `--c-line-alpha`, `--c-scrim`, `--hit-thumb`, `--w-slider-desktop`); zero cor fixa fora do `:root`; zero emoji; toque ≥ 44 px; piso 13/14 px; nenhuma classe sem uso ao final (as dos chips e da escala antiga saem); edições por `str_replace` com grep + `wc -l`; substituição de função inteira → grep por fragmentos órfãos; nenhum `console.log` com dado clínico.

## Decisões que este prompt implementa
- **Decisão 10 (usuário, 25/09) + dúvidas 3, 4 e 6 do Design (usuário, 26/09):** subgrupos em **lista com check redondo**, nunca chips em fileira; nas etapas **2, 3 e 4** o item marcado abre **uma barra deslizante 1–5** logo abaixo; nas etapas **1 e 5** o item marcado só acende na cor do tipo, **sem nota por extenso** (não há intensidade no dado); a barra **nasce sem valor** e a nota continua obrigatória; legenda a **13 px só abaixo de 360 px** (só a palavra que não cabe). Sugestão aceita: **tocar num ponto** também posiciona o marcador.
- **Decisão técnica (status, seção 4):** a barra é o **controle nativo de faixa** (`<input type="range" min="1" max="5" step="1">`) vestido com os tokens — thumb 44 px (`--hit-thumb`) com o número, trilha com preenchimento até o valor na cor do tipo, 5 pontos, legenda em grade de 5 colunas (`--t-legend`, `--w-likert-col`). Toque no ponto, arrasto, setas/Home/End/1–5 e leitor de tela vêm do controle nativo; `aria-valuetext` = "N · Palavra"; `aria-label` = "<rótulo> <item>". O estado "sem valor" é uma classe no contêiner (thumb e preenchimento invisíveis) que cai no primeiro `input`. Se o nativo não conseguir alguma medida do §6, registrar o desvio no relatório — não trocar por slider em JS.
- **D9:** obrigatório avisado **na própria tela**, junto do item/campo (aviso do estado *erro* do §6: rótulo em `--c-risk-ink` + frase de hoje, `role="alert"`); nenhum `alert()`.
- **Contrato v3 item 23:** Situação do **Positivo** = igual à do Negativo em verde-água, **sem** "Tipo de Contexto" e **sem** contagem no rodapé; botão "Emoções Agradáveis ›".
- **Débitos que fecham aqui:** **8.21** (corrida do cabeçalho cumulativo: quando o servidor responde tarde insere chaves vazias no registro seguinte — o cabeçalho novo lê só de `AUTO_STATE.dados` e não escreve nele) e **8.22** (título da barra corta em 390 px — a BarraEtapa §1 resolve com sobretítulo + título em até 2 linhas).

## Passos
1. **Gabarito ANTES** (item 4 do contexto). Inventário por grep, guardado no relatório: quantos elementos com `data-item` (esperado 225), quantos com `data-tem-likert`, quantos campos "Outro" (45), e todos os consumidores do valor de nota (função que monta "Item:nota", a que restaura rascunho, a que preenche a edição de registro antigo, o cabeçalho cumulativo, a revisão).
2. **GrupoRecolhível (§4):** cada um dos 5 grupos da etapa vira cartão recolhível: cabeça-botão "N. Título" com `aria-expanded`, à direita "5 itens" ou "N marcado(s)" (soma itens + "Outro" preenchido); aberto mostra a descrição do grupo em itálico (texto literal, com as aspas quando o HTML já as tem) e a ListaSubgrupo; um aberto por vez (abrir outro recolhe o anterior; o marcado permanece). Borda `--c-line` fechado, `-line` do tipo aberto.
3. **ListaSubgrupo (§5):** dentro do grupo aberto, os 5 subgrupos um abaixo do outro com `border-top --c-line-inner`; cada linha é um botão `role="checkbox"` de largura total, `min-height` 56, check redondo 28 à esquerda (marcado: fundo `reg` do tipo + `i-check`), nome 400 16 que quebra livre (nunca cortado); marcado com intensidade → nome 600 `--c-ink` + nota por extenso à direita ("3 · Moderado"; vazia até a barra ter valor); marcado sem intensidade (etapas 1 e 5) → nome 600 na cor `-ink` do tipo. Última linha "Outro:" com check + campo de borda inferior tracejada e apoio "especifique..." (some ao digitar; volta se vazio); com texto conta como marcado e, nas etapas com intensidade, ganha barra. **Os mesmos elementos continuam carregando `data-grupo`/`data-item`/`data-tem-likert`** — muda a apresentação, não a chave.
4. **BarraDeslizante (§6, decisão técnica acima):** aparece na mesma célula da lista, sob o item marcado com intensidade, antes do divisor seguinte. Rótulo em caixa alta na cor `-ink` do tipo — Negativo: "DESCONFORTO:" (Emoções), "MAL-ESTAR:" (Reações), "ACREDITO:" (Pensamentos); Positivo: "CONFORTO:", "BEM-ESTAR:", "ACREDITO:" — ou seja, os rótulos de hoje, em caixa alta por CSS. Trilha de 10 % a 90 %, 5 pontos, preenchimento até o valor, marcador 44 com o número, legenda por etapa vinda do HTML atual (Emoções "Nenhum…Intenso", Reações "…Extremo", Pensamentos "Nada…Totalmente"; Positivo idem, do que já está no HTML). Celular: sangria `margin 0 -12px` para caber 5 × 70 px; < 360 px a palavra que não cabe desce para 13 px. **Grava exatamente como hoje:** "Item:nota" com `AUTO_SEPARADOR`; desmarcar o item apaga a nota.
5. **Cabeçalho cumulativo (§3, contrato 17):** cartão `-tint`; linha HUMOR com `i-humor-N` + nome de `HUMOR_NOMES`; por etapa feita, ícone da etapa (`i-pin`, `i-heart-crack`/`i-heart`, `i-zap`/`i-leaf`, `i-cloud-rain`/`i-sun`, `i-trend-down`/`i-trend-up` — inserir no sprite os que faltarem, de `docs/design/16.5/rodada3/icones.js`), definição dos subgrupos marcados com a nota em `<b>`, depois a frase em itálico, em até 2 linhas cortando a partir da frase; Positivo: Situação só com a frase; "editar" (lápis; só com etapa feita) e "ver tudo" (camadas). Na etapa 1 só HUMOR + "ver tudo". **Fecha o 8.21:** o cabeçalho só lê `AUTO_STATE.dados`, nunca escreve; grep confirma que nenhuma função de render atribui em `AUTO_STATE.dados`.
6. **BarraEtapa + Trilho (§1, §2, contrato 15):** sobretítulo "REGISTRO NEGATIVO/POSITIVO" + título "Etapa N de 5 · nome completo" em até 2 linhas (`text-wrap:balance`) + "Salvar e sair"; trilho de 5 segmentos abaixo, só indicador, etapas feitas tocáveis. **Fecha o 8.22.** A checagem (AUT-03) usa a mesma barra com sobretítulo "AUTOMONITORAMENTO" — conferir que o título não corta em 390 px.
7. **RodapéEtapa (§9, contrato 15):** fixo no celular; contagem centrada acima dos botões ("1 situação marcada", "N emoções marcadas", "N reação(ões) física(s) marcada(s)", "N pensamento(s) marcado(s)", "N comportamento(s) marcado(s)"; "Nada marcado ainda" quando zero; **nenhuma contagem na Situação do Positivo**); "‹" 56 com `aria-label="Etapa anterior"`; primário "Nome da próxima etapa ›" / "Concluir e Revisar" — os textos que já existem. **Obrigatório (D9):** ao avançar sem o mínimo, o aviso aparece na tela junto do que falta (item marcado sem nota → estado erro da barra; Situação sem texto → aviso do campo), sem `alert()`.
8. **CartãoAjuda (§8):** na etapa 4 do Negativo nasce **recolhido** (título + chevron); conteúdo igual ao de hoje quando aberto.
9. **Situação do Positivo (contrato 23):** remover da etapa 1 do Positivo o bloco "Tipo de Contexto" (só do bloco `-b`; o `-a` do Negativo fica), sem contagem no rodapé, botão "Emoções Agradáveis ›". Se o bloco `-b` da Situação hoje tiver grupos com `data-grupo`, **não apagar as chaves**: ocultar o bloco e registrar no relatório (os grupos positivos são pacote futuro, sem previsão).
10. **Sai:** CSS e JS dos chips em fileira e da escala de 5 botões (classes, handlers, ícones exclusivos) — listar no relatório o que foi removido; grep final por nomes das classes antigas = 0.
11. **Consumidores da nota (lição 16.4.1):** re-auditar por grep todos os pontos do inventário do Passo 1 depois da mudança — restauração de rascunho (marca o item **e** posiciona a barra com o valor), edição de registro antigo (Meus Registros → editar), revisão, cabeçalho. Registro antigo com nota 1–5 tem de reabrir com a barra no valor certo.
12. **Textos:** só os da lista + aceitos do 16.5c + "Nada marcado ainda" e as contagens (já aceitos em 25–26/09). Nenhum texto novo; se precisar, parar e listar "hoje → proposta → motivo".
13. **Marcador:** linha 2 → "Pacote 16.5d-2 (data)".
14. **Validação:** `node --check`; tags; toda `var()` declarada; zero cor fixa fora do `:root`; zero emoji; zero classe sem uso; toque ≥ 44 px (linhas 56, check dentro do alvo da linha, thumb 44, botões 56); piso 13 px só na legenda < 360 px; **gabarito DEPOIS = ANTES**; contagens do inventário iguais (225 `data-item`, 45 "Outro", mesmo número de `data-tem-likert`); Playwright com Chrome visível 390×844, **360×780** (legenda) e 1280×800 (só não pode quebrar; o layout de computador é o 16.5f): login inválido até a mensagem do servidor, zero `pageerror`, comparando com `index.html`; sessão simulada do paciente de teste percorrendo **Negativo e Positivo ponta a ponta**: menu → etapa 1 (Situação; Positivo sem Tipo de Contexto) → etapa 2 (marcar 2 itens, arrastar uma barra, tocar num ponto da outra, teclado ← → em uma, tentar avançar com um item sem nota → aviso na tela) → etapa 3 → etapa 4 (ajuda recolhida; abrir e fechar) → etapa 5 (marcado só acende) → Revisar (chip "feita" por etapa — visual de hoje, o novo é o 16.5e) → **enviar de verdade um Negativo e um Positivo com o paciente de teste VMC no publicado**; "Salvar e sair" na etapa 3 → página → "Continuar registro" → etapa 3 com itens e barras restauradas; Meus Registros → editar um registro antigo → barras nos valores gravados. Capturas de cada etapa dos dois tipos em `../VMC-offline/capturas_16_5d2/` (390 e 1280) + a legenda em 360.
15. **Planilha (sem dado clínico no relatório):** nas duas linhas novas da aba Automonitoramento do VMC, mesmas colunas preenchidas que as linhas 53/54 de 26/09 e itens no formato "Item:nota" — conferir lendo de volta com `lerHistorico` (contagem de colunas e formato, não conteúdo).
16. **Publicar:** commit `Pacote 16.5d-2 - Etapas com lista de subgrupos, barra deslizante e cabecalho cumulativo (index-dev)` · `git push` · esperar o Pages · repetir login inválido e o fluxo do Negativo em `index-dev.html?v=165d2`.
17. **Docs:** `docs/arquitetura.md` (ListaSubgrupo, BarraDeslizante nativa, cabeçalho só-leitura, débitos 8.21/8.22 fechados), `docs/design/icones.md` (ícones novos do cabeçalho). Bug fora do escopo → registrar, não corrigir (16.5d-2.1).
18. **Relatório** `../VMC-offline/PACOTE_16_5d2_RELATORIO.md`: inventário antes/depois, o que saiu, desvios do §6 no controle nativo (se houver), decisão sobre o bloco `-b` da Situação, greps finais, gabarito igual, capturas, planilha (colunas), diferenças em relação ao `02_telas.md`.
19. Mensagem final: "publicado" + 5 linhas.

## Checklist do usuário (aba anônima, `index-dev.html?v=165d2`, paciente VMC)
- [ ] Em qualquer etapa, os grupos são cartões que abrem um por vez; fechados mostram "5 itens" ou "N marcado(s)".
- [ ] Dentro do grupo: os 5 subgrupos em lista com bolinha de marcar à esquerda e "Outro: especifique..." por último. Nenhum chip em fileira.
- [ ] Etapas 2, 3 e 4: ao marcar, aparece a barra de 1 a 5 logo abaixo, vazia; arrastar ou tocar num ponto escolhe a nota; a nota por extenso aparece à direita do nome ("3 · Moderado"); as palavras da legenda são as de sempre para cada etapa.
- [ ] Tentar avançar com um item marcado sem nota: aviso na própria tela, sem janela do navegador.
- [ ] Etapas 1 e 5: o item marcado só acende na cor do tipo, sem nota.
- [ ] Cabeçalho no topo mostra HUMOR com rostinho e, a cada etapa concluída, uma linha com o que foi marcado (notas em negrito, frase em itálico), com "editar" e "ver tudo".
- [ ] Título da barra ("Etapa 2 de 5 · Emoções Desagradáveis") não corta no celular; rodapé mostra "N … marcada(s)" e o nome da próxima etapa.
- [ ] Etapa 4: o cartão de ajuda começa fechado.
- [ ] Positivo, etapa 1: sem "Tipo de Contexto"; botão "Emoções Agradáveis ›".
- [ ] Um Negativo e um Positivo completos gravam; em Meus Registros → editar um registro antigo, as barras aparecem nas notas gravadas.
